# Set only after confirming the destination and its handling of lead data.
# Must receive JSON and return a successful HTTP status after storing the request.
OPERATIONS_REVIEW_WEBHOOK_URL=
# Optional: only the final, owner-confirmed HTTPS origin, without a path.
# No canonical/hreflang/social origin is inferred from Vercel previews.
# Indexing stays disabled while commercial and lead-delivery gates are unresolved.
SITE_URL=
