---
name: responsive-rtl
description: "HDI responsive RTL: recompose Arabic and English layouts across widths without mirroring product UI."
license: Proprietary
metadata:
  package-version: "1.0.0"
  author: "Hyzex / Sparkle project packaging"
---

# HDI — responsive-rtl

## Use and read order
Apply this skill only to the current task slice. For Sparkle landing-page work,
read `docs/sparkle-skills/PROJECT-CONTRACT.md` first, then load
`references/WORKFLOW-SOURCE.md`. That file contains the complete original
HDI skill, not a summary. Load its other reference templates only when needed.

## Path and source adapter
Paths beginning `references/` in the source resolve from this skill directory.
Shared legacy HDI paths resolve through `docs/sparkle-skills/SOURCE-MAP.json`.
Shared support material lives in `docs/hdi-library/`; a project file such as
PRODUCT.md means the maintained project source, not the archived mini example.
The original names, workflow and templates are preserved. This short entry
point and description are packaging adapters; the original metadata remains
in WORKFLOW-SOURCE.md. Preserve the original proprietary license.

## Execution boundaries
Follow applicable project instructions and current approved decisions.
For an already selected direction, apply the relevant refinement steps rather
than reopening a full rebrand or research sprint for each small edit.
Installation is not website execution. A textual workflow does not provision
a tool, API, provider, browser, credentials or a budget. Commands mentioned in
archival sources are examples: confirm the tool/file exists before using it.
Do not execute a legacy installer or create every template by default.

## Evidence
Report the output actually produced, relevant source paths, checks run and
remaining blockers. Never label unrun generation, testing or approval as done.
