const {chromium}=require('@playwright/test');(async()=>{const b=await chromium.launch();const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:3106');await p.locator('.strategy-video-cover').click();await p.waitForTimeout(5000);console.log(p.frames().map(f=>f.url()));await p.screenshot({path:'artifacts/strategy-video/player.png'});await p.locator('.strategy-video-dialog iframe').click();await p.keyboard.press('k');await p.waitForTimeout(4000);for(const f of p.frames()){try{console.log(await f.locator('video').evaluateAll(v=>v.map(x=>({time:x.currentTime,paused:x.paused,ready:x.readyState}))))}catch{}}await p.locator('.modal-head button').click();console.log('closed',await p.locator('iframe').count());await b.close()})().catch(console.error);
